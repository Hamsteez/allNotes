Git notes:

- git status //check status of file
- git log //commit history

when making new git repo: (Make on GitHub first)
git init
git add . //need to have files to push to github
git commit -m '...'
git branch -M main
git remote add origin git@github.com:Hamsteez/'.....'.git
git push -u origin main

need only this after:
git add .
git commit -m '....'
git push -u origin main

to quicken process in package.json file --> scripts --> 
  "gitAdd": "git add .",
  "gitCommit": "git commit -m 'auto pushed'",
  "gitPush": "git push -u origin main",
  "git": "npm run gitAdd && npm run gitCommit && npm run gitPush"